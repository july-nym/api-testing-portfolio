# staging profile (activate with: mvn test -Pstaging -Denv=staging)
# Points at the same public sandboxes here; in a real project these would be the
# staging hosts. Kept distinct so the profile wiring is demonstrable end to end.
reqres.baseUrl=https://reqres.in/api
reqres.apiKey=reqres-free-v1
booker.baseUrl=https://restful-booker.herokuapp.com
maxResponseMs=3000
